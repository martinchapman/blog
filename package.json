{
  "type": "module",
  "devDependencies": {
    "@shopify/prettier-plugin-liquid": "^1.9.4",
    "html-validate": "^10.0.0",
    "prettier": "^3.6.2",
    "stylelint": "^16.23.1",
    "stylelint-config-standard": "^39.0.0"
  },
  "scripts": {
    "test": "jest",
    "format": "prettier --write .",
    "lint": "html-validate . && stylelint assets/stylesheets/*.css",
    "prettier": "npm run format && npm run lint"
  }
}
